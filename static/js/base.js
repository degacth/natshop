// Generated by CoffeeScript 1.9.2
(function() {
  var fixed_menu;

  fixed_menu = $('#fixed-menu');

  $(window).scroll(function() {
    var $a, pos, top_class;
    top_class = 'on-top';
    $a = $(this);
    pos = Math.max(0, 169 - $a.scrollTop());
    fixed_menu.css('top', pos);
    if (pos) {
      return fixed_menu.removeClass(top_class);
    } else {
      return fixed_menu.addClass(top_class);
    }
  }).trigger('scroll');

}).call(this);
